#include "main.h"


void initialise_all_tables(int n)
{
    /* in this section, we intialise all the tables with the app  func*/

    for(int i = 0; i<n ; i++)
    {
        // init all chef threads and get them to function
    }
    printf("all tables are set up and serving\n");
    return;
}